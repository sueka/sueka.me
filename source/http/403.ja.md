---
layout: page
title: 403 Forbidden
interlanguage-id: 403
lang: ja
---

<i>{{ site.email }}</i> ([0x{{ site.pgp_key_id }}]({{ site.pks_url }})) で電子メールを受信してゐます。

<blockquote cite="https://tools.ietf.org/html/rfc7231" lang="en" class="display-none">
  <p>The 403 (Forbidden) status code indicates that the server understood the request but refuses to authorize it.  A server that wishes to make public why the request has been forbidden can describe that reason in the response payload (if any).</p>
  <p>If authentication credentials were provided in the request, the server considers them insufficient to grant access.  The client <strong class="plain">SHOULD NOT</strong> automatically repeat the request with the same credentials.  The client <strong class="plain">MAY</strong> repeat the request with new or different credentials.  However, a request might be forbidden for reasons unrelated to the credentials.</p>
  <p>An origin server that wishes to "hide" the current existence of a forbidden target resource <strong class="plain">MAY</strong> instead respond with a status code of 404 (Not Found).</p>
</blockquote>

<div class="blockquote-like">
  <p>ステータスコード 403 (Forbidden) は、サーバーは、リクエストを理解しはしたが、その認可は拒絶したといふことを表します。リクエストを許可しなかった理由を公開したい場合、サーバーは、それを（あれば）レスポンスのペイロードに記述することができます。</p>
  <p>リクエストの中で認証資格情報が提供されてゐたとしても、サーバーは、アクセスを許可するには不十分と見做します。クライアントは、同じ資格情報によるリクエストを自動的に繰り返す<strong class="plain">べきではありません</strong> (<strong class="plain">SHOULD NOT</strong>) 。クライアントは、新しい、または異なる資格情報を用ゐてゞあれば、リクエストを繰り返す<strong class="plain">ことができます</strong> (<strong class="plain">MAY</strong>) 。たゞし、リクエストの拒否は、資格情報に無関係な理由によるものかもしれません。</p>
  <p>配信元のサーバーは、禁止されたターゲットリソースが現時点で存在してゐるかどうかを「隠し」たい場合、代はりに 404 (Not Found) といふステータスコードを返し<strong class="plain">てもよいです</strong> (<strong class="plain">MAY</strong>) 。</p>
</div>
